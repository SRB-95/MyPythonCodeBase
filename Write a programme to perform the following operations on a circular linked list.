Write a programme to perform the following operations on a circular linked list.
a)Add a new element at the appropriate position in a  linked list.
b)Search an element in the linked list and delete it.

Solution:

list = ["apple","banana","cherry"]
list.append("orange")
a=input("Enter a string :")
for i in list:
    if i == a:
        list.remove(a) 
print(list)

Output:

Enter a string :apple
['banana', 'cherry', 'orange']
